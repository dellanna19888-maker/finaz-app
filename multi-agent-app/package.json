{
  "name": "multi-agent-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Eigenständige Multi-Agent-KI-App auf Basis der Claude API (Orchestrator + spezialisierte Agenten).",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
